import { TodoPaginationPipe } from './todo-pagination.pipe';

describe('TodoPaginationPipe', () => {
  it('create an instance', () => {
    const pipe = new TodoPaginationPipe();
    expect(pipe).toBeTruthy();
  });
});
